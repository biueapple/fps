using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Stone : Inter
{
    // Start is called before the first frame update
    void Start()
    {
        //breaking(this, 10);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
